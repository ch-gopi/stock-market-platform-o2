apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-initdb
data:
  init.sql: |
    CREATE DATABASE watchlistdb;
    CREATE DATABASE historicaldb;
    CREATE DATABASE usersdb;
    CREATE DATABASE marketsearchdb;









