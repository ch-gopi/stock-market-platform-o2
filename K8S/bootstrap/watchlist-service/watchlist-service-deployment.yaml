apiVersion: apps/v1
kind: Deployment
metadata:
  name: watchlist-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: watchlist-service
  template:
    metadata:
      labels:
        app: watchlist-service
    spec:
      containers:
      - name: watchlist-service
        image: gopi/watchlist-service:latest
        ports:
        - containerPort: 8076
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: k8
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8076
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8076
          initialDelaySeconds: 10
          periodSeconds: 5
