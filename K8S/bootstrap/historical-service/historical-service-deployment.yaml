apiVersion: apps/v1
kind: Deployment
metadata:
  name: historical-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: historical-service
  template:
    metadata:
      labels:
        app: historical-service
    spec:
      containers:
      - name: historical-service
        image: gopi/historical-service:latest
        ports:
        - containerPort: 8094
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: k8
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8094
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8094
          initialDelaySeconds: 10
          periodSeconds: 5



